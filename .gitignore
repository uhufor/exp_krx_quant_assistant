# =============================================================================
# Python
# =============================================================================
__pycache__/
*.py[cod]
*.pyd
*.so
*.egg-info/
.venv/
dist/
build/

# =============================================================================
# 도구 캐시
# =============================================================================
.pytest_cache/
.mypy_cache/
.ruff_cache/

# =============================================================================
# 런타임 / 데이터
# =============================================================================
# DuckDB 데이터베이스 (data/ 구조는 유지, 실제 DB 파일만 제외)
data/*.duckdb
data/*.duckdb.wal

# 일별 리포트 출력물 — 반드시 루트 한정(/)이어야 한다.
# `reports/`로 두면 소스 디렉터리 src/quant_krx/reports/ 까지 무시되어 렌더러 모듈이
# 통째로 버전 관리에서 빠진다(실제로 그런 상태였음).
/reports/

# 로그(루트 한정 — 위와 같은 이유)
/logs/

# =============================================================================
# 설정 / 비밀키
# =============================================================================
.env
# watchlist는 개인 설정 — 샘플(watchlist.yaml.example)만 커밋
config/watchlist.yaml

# =============================================================================
# macOS
# =============================================================================
.DS_Store
.AppleDouble

# =============================================================================
# IDE
# =============================================================================
.vscode/
.idea/
*.swp
*.swo

# =============================================================================
# OMC (oh-my-claudecode) 운영 상태 파일
# .omc/skills/** 는 프로젝트 스킬로 커밋 대상
# =============================================================================
.omc/state/
.omc/sessions/
.omc/logs/
.omc/artifacts/
.omc/notepad.md
.omc/sessions/
